# 项目工作状态存档 — 2026-06-28

## 当前阶段

LLM fallback provider 加固（`llm-fallback-provider-hardening`）分支任务：**修复阶段，已实现完成但尚未合并到 origin**。

---

## 已完成的 4 项修复

| 优先级 | 问题 | 方案 | 涉及文件 | 验证 |
|---|---|---|---|---|
| P0 | `aclose()` 对 `__new__` 构造对象不安全（`_closed` 未初始化） | `getattr(self, '_closed', False)` 安全守卫 | `app/services/llm_gateway.py:311` | pytest 25/25 |
| P1 | `PipelineRunner` 无条件关闭外部注入网关 | `_owns_llm` 所有权追踪，`finally` 只关闭自有网关 | `app/services/orchestrator.py` `__init__` + `run()` | 31/31 + 27/27 + 7/7，调用方审计确认 |
| P1 | 缓存隔离不完整（fallback 结果污染主缓存键、config_version 重启回零、键不含 fallback 信息） | 3a: fallback 成功写入 fallback 专属键 / 3b: 内容哈希替代递增版本 / 3c: 缓存键包含 fallback 信息 | `app/services/llm_gateway.py` (chat + _cache_key), `app/services/llm_config.py` (refresh_cache + get_config_version) | pytest 25/25 |
| P1 | 前端清空备用配置时发送 `undefined`，后端不更新 | 前端 `fbKey === '' ? '' : (fbKey \|\| undefined)` + 后端 `_load_from_db` 区分空串与 None | `frontend/src/pages/Settings.tsx:428-429`, `app/services/llm_config.py:46-48` | npm run build 通过，后端链路验证通过 |

**变更汇总（git diff --stat）**：11 个文件，+465 / -86 行。

---

## 全量回归结果

```
# 排除前置测试污染源后
pytest tests/ --ignore=tests/test_agentic_router_entrypoints.py
→ 496 passed, 0 failed

# 前端构建
npm run build → 成功

# 空白错误检查
git diff --check → 通过

# Unclosed client session 警告
已消除（无此警告）
```

---

## 待验证 / 遗留事项

| 事项 | 类型 | 建议处理时机 |
|---|---|---|
| `test_agentic_router_entrypoints.py` mock 不完整（缺 `llm_task_context`），阻塞 68 个测试 | 前置测试污染（非本次改动引入） | 单独修复，PR 前或合入后均可 |
| 未覆盖 fallback 实际调用成功的端到端集成测试 | 测试缺口 | 下一步 |
| 未覆盖主/备均失败的集成测试 | 测试缺口 | 下一步 |
| 未覆盖缓存切换和配置失效的集成测试 | 测试缺口 | 下一步 |
| `test_agentic_router_entrypoints.py` 已在本次改动中移除了 `sys.modules` mock（-26 行） | ✅ 已修复 | 已完结 |

---

## 数据库

| 内容 | 状态 |
|---|---|
| 数据库备份 | `backups/support_ai_2026-06-27.sql`（旧）、`backups/support_ai_2026-06-28.sql`（新） |

数据库连接（docker compose）：`rag_search-postgres-1`，用户 `postgres`，库 `support_ai`，端口映射 `127.0.0.1:5433`。

---

## 当前 git 状态

- **分支**：`main`
- **领先 origin/main**：3 个 commit
- **未提交改动**：11 个已修改文件 + `.scratch/llm-fallback-provider-hardening/`（任务工作记录）+ `backups/`（数据库备份）
- **最近提交**：`0599214 存档 RAG_Search 2026-06-27 22:24`

---

## 本地 git 路径

```
D:\develop\Git
```

---

## 下一步建议

1. **提交当前改动到本地 git**，包含所有修复 + 存档文档 + 数据库备份
2. 合并到 remote 前，可选修复 `test_agentic_router_entrypoints.py` 污染（+36 行 `llm_task_context` 即可）
3. 补充 fallback 成功/失败/缓存切换的集成测试
4. 合并到 remote（需用户确认）
