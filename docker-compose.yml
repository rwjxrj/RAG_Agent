services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_KEY: ${API_KEY:-}
        VITE_ADMIN_API_KEY: ${ADMIN_API_KEY:-}
    ports:
      - "5174:80"
    depends_on:
      - api

  nginx:
    image: docker.m.daocloud.io/library/nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api
    profiles:
      - full

  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: support-ai-assistant:local
    ipc: host
    ports:
      - "8000:8000"
    volumes:
      - ./source:/app/source
    environment:
      - PLAYWRIGHT_BROWSERS_PATH=/ms-playwright
      - PLAYWRIGHT_INSTALL_ON_STARTUP=false
      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/support_ai
      - DATABASE_URL_SYNC=postgresql://postgres:postgres@postgres:5432/support_ai
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=redis://redis:6379/1
      - OPENSEARCH_HOST=http://opensearch:9200
      - QDRANT_HOST=qdrant
      - QDRANT_PORT=6333
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - OPENAI_BASE_URL=${OPENAI_BASE_URL:-}
      - LLM_MODEL=${LLM_MODEL:-gpt-5.2}
      - LLM_FALLBACK_MODEL=${LLM_FALLBACK_MODEL:-gpt-5.2}
      - LLM_MODEL_ECONOMY=${LLM_MODEL_ECONOMY:-gpt-5.2}
      - LLM_MAX_TOKENS=2048
      - RERANKER_PROVIDER=${RERANKER_PROVIDER:-custom}
      - API_KEY=${API_KEY:-}
      - ADMIN_API_KEY=${ADMIN_API_KEY:-}
      - JWT_SECRET=${JWT_SECRET:?JWT_SECRET must be set in .env}
      - OBJECT_STORAGE_URL=http://minio:9000
      - OBJECT_STORAGE_ACCESS_KEY=${MINIO_ACCESS_KEY:-minioadmin}
      - OBJECT_STORAGE_SECRET_KEY=${MINIO_SECRET_KEY:-minioadmin}
      - OBJECT_STORAGE_BUCKET=support-ai-docs
      - MAX_REQUEST_BODY_BYTES=1048576
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
      opensearch:
        condition: service_healthy
      qdrant:
        condition: service_started
      minio:
        condition: service_started
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    image: support-ai-assistant:local
    ipc: host
    volumes:
      - ./source:/app/source
    environment:
      - PLAYWRIGHT_BROWSERS_PATH=/ms-playwright
      - PLAYWRIGHT_INSTALL_ON_STARTUP=false
      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/support_ai
      - DATABASE_URL_SYNC=postgresql://postgres:postgres@postgres:5432/support_ai
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=redis://redis:6379/1
      - OPENSEARCH_HOST=http://opensearch:9200
      - QDRANT_HOST=qdrant
      - QDRANT_PORT=6333
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - OPENAI_BASE_URL=${OPENAI_BASE_URL:-}
      - LLM_MODEL=${LLM_MODEL:-gpt-5.2}
      - LLM_FALLBACK_MODEL=${LLM_FALLBACK_MODEL:-gpt-5.2}
      - LLM_MODEL_ECONOMY=${LLM_MODEL_ECONOMY:-gpt-5.2}
      - RERANKER_PROVIDER=${RERANKER_PROVIDER:-custom}
      - API_KEY=${API_KEY:-}
      - ADMIN_API_KEY=${ADMIN_API_KEY:-}
      - JWT_SECRET=${JWT_SECRET:?JWT_SECRET must be set in .env}
      - OBJECT_STORAGE_URL=http://minio:9000
      - OBJECT_STORAGE_ACCESS_KEY=${MINIO_ACCESS_KEY:-minioadmin}
      - OBJECT_STORAGE_SECRET_KEY=${MINIO_SECRET_KEY:-minioadmin}
      - OBJECT_STORAGE_BUCKET=support-ai-docs
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
      opensearch:
        condition: service_healthy
      qdrant:
        condition: service_started
      minio:
        condition: service_started
    command: celery -A worker.celery_app worker --loglevel=info

  postgres:
    image: docker.m.daocloud.io/library/postgres:15-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: support_ai
    ports:
      - "127.0.0.1:5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 10s
      retries: 10
      start_period: 30s

  redis:
    image: docker.m.daocloud.io/library/redis:7-alpine
    ports:
      - "127.0.0.1:6380:6379"
    volumes:
      - redis_data:/data

  opensearch:
    image: docker.m.daocloud.io/opensearchproject/opensearch:2.11.0
    environment:
      - discovery.type=single-node
      - bootstrap.memory_lock=false
      - "OPENSEARCH_JAVA_OPTS=-Xms1g -Xmx1g"
      - plugins.security.disabled=true
    ports:
      - "127.0.0.1:19200:9200"
      - "127.0.0.1:19600:9600"
    volumes:
      - opensearch_data:/usr/share/opensearch/data
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9200/_cluster/health || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 15
      start_period: 60s

  qdrant:
    image: docker.m.daocloud.io/qdrant/qdrant:v1.12.0
    ports:
      - "127.0.0.1:6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage

  minio:
    image: docker.m.daocloud.io/minio/minio@sha256:14cea493d9a34af32f524e538b8346cf79f3321eff8e708c1e2960462bd8936e
    ports:
      - "127.0.0.1:9000:9000"
      - "127.0.0.1:9001:9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:-minioadmin}
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"

  minio-init:
    image: docker.m.daocloud.io/minio/mc@sha256:a7fe349ef4bd8521fb8497f55c6042871b2ae640607cf99d9bede5e9bdf11727
    depends_on:
      - minio
    entrypoint: >
      /bin/sh -c "
      sleep 5;
      mc alias set myminio http://minio:9000 ${MINIO_ACCESS_KEY:-minioadmin} ${MINIO_SECRET_KEY:-minioadmin};
      mc mb myminio/support-ai-docs --ignore-existing;
      exit 0;
      "

volumes:
  postgres_data:
  redis_data:
  opensearch_data:
  qdrant_data:
  minio_data:
